// create an empty array
  // use the range function to set the start and end numbers
  function between(a, b) {
    var ans = [];
    for (let i = a; i <= b; i++) {
        ans.push(i);
    }
    return ans;
}